#include "main.h"
/**Author: Adeboye Victor Oyekanmi*/

/**
 * swap_int - swaps the value of two integers
 * @ab: pointer to swap
 */
void swap_int(int *a, int *b);
{
	temp = *a;
	*a = *b;
	*b = temp;
}
